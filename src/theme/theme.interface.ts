export interface AppThemeColors {
    background: string;
    color: string;
    inline: string;
    outline: string;
    primary: string;
    secondary: string;
}

export interface AppThemeFont {
    size: {
        small: number;
        medium: number;
        large: number;
    }
}
